import SwiftUI

// This file is a placeholder to satisfy any lingering Xcode references.
// The main app entry point is now ZenithMainView.
struct ContentView: View {
    var body: some View {
        Text("If you see this, something went wrong. Please restart the app.")
    }
}
